export const metadata = {
  title: "Bikaner Fort | Bikaner Architecture | Bikaner | Rajasthan",
  description: "The Bikaner architecture is so beautiful that your heart will long to stay back at Bikaner Rajasthan. Bikaner Fort history is no exception.",
};

export default function Layout({ children }) {
  return <>{children}</>;
}